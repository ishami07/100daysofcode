// write a function take user as input and greet them with there name and age

function greet(user){
    console.log("Hii ! " + user.name+" your age is "+user.age);
}
 let user = {
    name:"shami",
    age:17 ,
    gander: "male"
 };
 greet(user);

